import { PropertymatcherPipe } from './propertymatcher.pipe';

describe('PropertymatcherPipe', () => {
  it('create an instance', () => {
    const pipe = new PropertymatcherPipe();
    expect(pipe).toBeTruthy();
  });
});
