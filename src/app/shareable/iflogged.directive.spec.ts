import { IfloggedDirective } from './iflogged.directive';

describe('IfloggedDirective', () => {
  it('should create an instance', () => {
    const directive = new IfloggedDirective();
    expect(directive).toBeTruthy();
  });
});
