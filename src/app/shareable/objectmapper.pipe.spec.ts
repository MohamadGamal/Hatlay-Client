import { ObjectmapperPipe } from './objectmapper.pipe';

describe('ObjectmapperPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectmapperPipe();
    expect(pipe).toBeTruthy();
  });
});
